<h1><%= @user.name %></h1>
<img src="<%= @user.photo %>" alt="<%= @user.name %>">
<p><%= @user.bio %></p>

<h2>Posts by <%= @user.name %></h2>
<ul>
  <% @user.posts.each do |post| %>
    <li>
      <%= link_to "#{post.title} #{post.text}", user_post_path(@user, post) %>
    </li>
  <% end %>
</ul>
